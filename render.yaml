services:
  - type: web
    name: frontend
    plan: starter
    runtime: node
    buildCommand: |
      npm install
      npm run build --workspace=@project/shared
      npm run build --workspace=@project/frontend
    startCommand: npm start --workspace=@project/frontend

  - type: web
    name: backend
    plan: starter
    runtime: node
    buildCommand: |
      npm install
      npm run build --workspace=@project/shared
      npm run build --workspace=@project/backend
    startCommand: npm start --workspace=@project/backend
